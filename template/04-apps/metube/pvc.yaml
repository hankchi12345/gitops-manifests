apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metube-downloads
  namespace: app-dev
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
