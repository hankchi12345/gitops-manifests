apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pretend-aws
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/hankchi12345/pretend-AWS.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: app-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
